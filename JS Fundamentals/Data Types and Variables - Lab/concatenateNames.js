function concatenateNames(nameOne, nameTwo, dlimeter) {
    console.log(nameOne + dlimeter + nameTwo);
}

concatenateNames('John', 'Smith', '->');
concatenateNames('Jan', 'White', '<->');
concatenateNames('Linda', 'Terry', '=>');